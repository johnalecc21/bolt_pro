// Loaded on demand by <LazyMotion> so the animation engine stays out of the
// first download. The landing uses no layout animations, so domAnimation is enough.
export { domAnimation as default } from "framer-motion";
